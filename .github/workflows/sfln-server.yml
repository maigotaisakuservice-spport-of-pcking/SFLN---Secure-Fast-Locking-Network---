name: SFLN Server & Dynamic DNS Update

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Server run duration (minutes)'
        required: true
        default: '60'
      cname_update:
        description: 'Update sfln-server.pdg.f5.si CNAME to tunnel URL?'
        type: boolean
        default: true

jobs:
  server:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        pip install -r sfln/server/requirements.txt
        wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
        sudo dpkg -i cloudflared-linux-amd64.deb

    - name: Start SFLN Server in Background
      run: |
        python3 sfln/server/main.py > sfln_server.log 2>&1 &
        echo "SFLN Server started."

    - name: Start Cloudflare Tunnel
      id: tunnel
      run: |
        # Tunneling the WebSocket port 9001
        cloudflared tunnel --url http://localhost:9001 > tunnel.log 2>&1 &

        for i in {1..10}; do
          TUNNEL_URL=$(grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' tunnel.log | head -n 1)
          if [ ! -z "$TUNNEL_URL" ]; then break; fi
          echo "Waiting for tunnel URL... ($i)"
          sleep 5
        done

        if [ -z "$TUNNEL_URL" ]; then
          echo "Failed to get tunnel URL."
          cat tunnel.log
          exit 1
        fi

        WS_TUNNEL_URL=$(echo $TUNNEL_URL | sed 's/https:\/\//wss:\/\//')
        echo "TUNNEL_URL=$TUNNEL_URL" >> $GITHUB_ENV
        echo "WS_TUNNEL_URL=$WS_TUNNEL_URL" >> $GITHUB_ENV
        echo "Tunnel URL: $TUNNEL_URL"

    - name: Update js/server-config.js
      run: |
        mkdir -p js
        # Provide both direct tunnel (valid SSL) and CNAME (may have SSL mismatch)
        echo "const SFLN_SERVER_CONFIG = { tunnel: '${{ env.WS_TUNNEL_URL }}', cname: 'wss://sfln-server.pdg.f5.si', https_tunnel: '${{ env.TUNNEL_URL }}' };" > js/server-config.js

    - name: Update f5.si Dynamic DNS (CNAME)
      if: ${{ github.event.inputs.cname_update == 'true' }}
      run: |
        CNAME_VAL=$(echo ${{ env.TUNNEL_URL }} | sed 's/https:\/\///')
        DOMAIN="sfln-server.pdg"
        PASSWORD="${{ secrets.F5_SI_API_TOKEN }}"
        curl -s "https://f5.si/update.php?domain=$DOMAIN&password=$PASSWORD&cname=$CNAME_VAL"

    - name: Keep Server Running
      run: |
        echo "SFLN Server is active."
        echo "Direct Tunnel (Recommended for Demo): ${{ env.WS_TUNNEL_URL }}"
        echo "Custom Domain (CNAME): wss://sfln-server.pdg.f5.si"
        sleep ${{ github.event.inputs.duration }}m
